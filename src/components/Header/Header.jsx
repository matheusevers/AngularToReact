import React from "react";
import Grid from "@material-ui/core/Grid";

import "./Header.css";

const Header = () => {
  return (
    <Grid item xs={12} className="header-heading">
      INVOICE
    </Grid>
  );
};

export default Header;
